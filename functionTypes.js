//simpler way to write the code below using "Arrow Function"
const getSum = (x, y) => x + y;

console.log(getSum(100, 200));

/*
//writing the codes below using "Arrow Function"
const getSum = (x, y) => {
  return x + y;
};

console.log(getSum(100, 200));
*/

/*
//simpler way to write the code below
function getSum(x, y) {
  return x + y;
}

console.log(getSum(100, 200));
*/

/*
let x = 100;
let y = 200;

function getSum(x, y) {
  return x + y;
}

console.log(getSum(x, y));
*/
